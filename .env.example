# === BASE DE DATOS ===
# Vercel Postgres / Supabase / PostgreSQL local
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/newsoft_sales?schema=public"

# Para Vercel Postgres (descomentar si usas Vercel)
# POSTGRES_PRISMA_URL="..."
# POSTGRES_URL_NON_POOLING="..."

# === AUTENTICACIÓN (NextAuth.js) ===
# Generar con: openssl rand -base64 32
NEXTAUTH_SECRET="tu-secreto-muy-seguro-aqui"
NEXTAUTH_URL="http://localhost:3000"

# === CREDENCIALES DE ACCESO (MVP) ===
# Usuarios del sistema — separar por coma si hay múltiples
ADMIN_EMAIL="roldan@newsoft.mx"
ADMIN_PASSWORD="cambiar-en-produccion"
SALES_EMAIL="elva@newsoft.mx"
SALES_PASSWORD="cambiar-en-produccion"
